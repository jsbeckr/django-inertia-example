# Django InertiaJS Example

## Requirements

* Node
* pipenv

## Installation

* `npm i`
* `pipenv install`
  * `pipenv shell`
  * `./manage.py migrate`

## Run

* `npm start`
* `pipenv shell`
  * `./manage.py runserver`